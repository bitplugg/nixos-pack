#!/usr/bin/env bash
# description: Check password status for users
# version: 0.1.0
# author: bitplugg
for u in $(awk -F: '$3>=1000 && $3!=65534 {print $1}' /etc/passwd 2>/dev/null | head -10); do
  echo -n "$u: "
  passwd -S "$u" 2>/dev/null | awk '{print $2, $3}' || echo "unknown"
done

#!/usr/bin/env bash
# description: Run rootkit hunter
# version: 0.1.0
# author: bitplugg
if which rkhunter >/dev/null 2>&1; then
  rkhunter --check --skip-keypress 2>/dev/null | tail -20
else
  echo "rkhunter not installed"
fi

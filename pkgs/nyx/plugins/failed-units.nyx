#!/usr/bin/env bash
# description: Show failed systemd units
# version: 0.1.0
# author: bitplugg
systemctl list-units --state=failed --no-legend 2>/dev/null | awk '{print $1, $3}' || echo 'no failed units'
echo "---"
systemctl --failed 2>/dev/null | tail -20

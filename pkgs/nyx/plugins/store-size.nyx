#!/usr/bin/env bash
# description: Show nix store size breakdown
# version: 0.1.0
# author: bitplugg
echo "Top store directories:"
du -sh /nix/store/*/ 2>/dev/null | sort -rh | head -20
echo "---"
echo "Total: $(du -sh /nix/store 2>/dev/null | cut -f1)"

#!/usr/bin/env bash
# description: List system users
# version: 0.1.0
# author: bitplugg
awk -F: '{print \$1, \$3, \$6}' /etc/passwd 2>/dev/null | head -30 || echo 'no passwd'
echo "---"
echo "Total users: $(cat /etc/passwd 2>/dev/null | wc -l)"

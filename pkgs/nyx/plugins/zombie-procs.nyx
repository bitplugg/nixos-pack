#!/usr/bin/env bash
# description: Show zombie processes
# version: 0.1.0
# author: bitplugg
ps aux | awk '$8 ~ /Z/ {print \$2, \$11, \$8}' || echo 'no zombies'
echo "Zombie count: $(ps aux | awk '$8 ~ /Z/' | wc -l)"

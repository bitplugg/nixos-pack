#!/usr/bin/env python3
# description: Generate UUID
# version: 0.1.0
# author: bitplugg
import uuid, sys
count = int(sys.argv[1]) if len(sys.argv) > 1 else 1
for _ in range(count):
    print(uuid.uuid4())
